import React from 'react';
import { withRouter } from "react-router-dom"; 

const Contacto = () => {
    return (
        <div>
            <h1>Contacto</h1>
        </div>
    );
};

export default withRouter(Contacto);